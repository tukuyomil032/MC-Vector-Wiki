---
title: File Manager
description: Browse, edit, and organize your server files directly from MC-Vector.
category: features
order: 4
---

<En>

MC-Vector includes a full-featured **File Manager** that lets you interact with your server's files without leaving the app. It integrates with the **Monaco Editor** for in-app text editing.

## Navigating Files

Open the **Files** tab in any server to browse the directory tree. Click folders to expand them; click text files to open them in the editor.

## Supported Operations

| Operation | How to use |
|-----------|------------|
| **Create** | Click `+` to create a new file or folder |
| **Edit** | Click any `.properties`, `.yml`, `.json`, `.toml`, etc. file to open in Monaco |
| **Rename / Move** | Right-click an item |
| **Delete** | Right-click → Delete |
| **Compress** | Select items → Compress to ZIP |
| **Extract** | Right-click a `.zip` → Extract |
| **Import** | Click the Import button to open your OS file picker and copy files in |
| **Open in Finder/Explorer** | Opens the current folder in your OS file browser |

## Monaco Editor

MC-Vector uses the **Monaco Editor** (the same engine as VS Code) for all in-app text editing:

- Syntax highlighting for JSON, YAML, TOML, and more.
- Line numbers and error indicators.
- Save with `Ctrl+S` (Windows) / `Cmd+S` (macOS).

<div className="bg-blue-900/20 border border-blue-500/50 rounded-md p-4 my-6">
<div className="text-blue-400 font-bold mb-1">Tip</div>
<div className="text-sm text-gray-300">To install plugins or mods, use the built-in <strong>Plugins/Mods</strong> tab — it's faster than manually copying files here.</div>
</div>

## Multi-select

Hold **Shift** or **Ctrl/Cmd** and click to select multiple files at once, then compress or delete them in a single action.

</En>

<Ja>

MC-Vectorには、アプリを離れることなくサーバーのファイルを操作できる高機能な**ファイルマネージャー**が内蔵されています。テキスト編集には**Monaco Editor**が統合されています。

## ファイルの操作

任意のサーバーで**Files**タブを開くとディレクトリツリーを閲覧できます。フォルダーをクリックして展開し、テキストファイルをクリックするとエディターで開きます。

## 対応操作

| 操作 | 方法 |
|------|------|
| **新規作成** | `+`ボタンでファイルまたはフォルダーを作成 |
| **編集** | `.properties`、`.yml`、`.json`などのファイルをクリックしてMonacoで開く |
| **名前変更/移動** | 右クリック |
| **削除** | 右クリック → 削除 |
| **圧縮** | アイテムを選択 → ZIPに圧縮 |
| **展開** | `.zip`ファイルを右クリック → 展開 |
| **インポート** | インポートボタンでOSのファイルピッカーを開いてファイルをコピー |
| **Finder/エクスプローラーで開く** | 現在のフォルダーをOSのファイルブラウザで開く |

## Monaco Editor

MC-Vectorはテキスト編集にVS Codeと同じエンジンの**Monaco Editor**を使用しています：

- JSON、YAML、TOMLなどのシンタックスハイライト。
- 行番号とエラー表示。
- `Ctrl+S`（Windows）/ `Cmd+S`（macOS）で保存。

<div className="bg-blue-900/20 border border-blue-500/50 rounded-md p-4 my-6">
<div className="text-blue-400 font-bold mb-1">ヒント</div>
<div className="text-sm text-gray-300">プラグインやModをインストールするには、ファイルを手動でコピーするよりも内蔵の<strong>Plugins/Mods</strong>タブを使う方が簡単です。</div>
</div>

## 複数選択

**Shift**または**Ctrl/Cmd**を押しながらクリックして複数のファイルを選択し、一度に圧縮や削除ができます。

</Ja>

